# Nixpacks configuration for Railway API deployment
# Using explicit commands to avoid workspace auto-detection issues

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["cd api && npm run build"]

[start]
cmd = "node api/dist/index.js"
