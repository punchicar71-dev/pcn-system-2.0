# Nixpacks configuration for Railway deployment
# Explicitly configure build and start to avoid workspace auto-detection issues

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build --workspace=pcn-api"]

[start]
cmd = "npm run start"
