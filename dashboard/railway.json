{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build --workspace=pcn-dashboard"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "npm run start:prod --workspace=pcn-dashboard",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
