{
  "name": "pcn-vehicle-system",
  "version": "2.0.0",
  "description": "Vehicle Selling Management System with Website",
  "private": true,
  "workspaces": [
    "dashboard",
    "web",
    "api",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:dashboard\" \"npm run dev:web\" \"npm run dev:api\"",
    "dev:dashboard": "cd dashboard && npm run dev",
    "dev:web": "cd web && npm run dev",
    "dev:api": "cd api && npm run dev",
    "build": "npm run build:dashboard && npm run build:web && npm run build:api",
    "build:dashboard": "cd dashboard && npm run build",
    "build:web": "cd web && npm run build",
    "build:api": "cd api && npm run build",
    "lint": "npm run lint:dashboard && npm run lint:web && npm run lint:api",
    "lint:dashboard": "cd dashboard && npm run lint",
    "lint:web": "cd web && npm run lint",
    "lint:api": "cd api && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
